import 'package:flutter/material.dart';

class SamsColors {
  // Portal Theme (Used for Login/Register)
  static const List<Color> portalGradient = [
    Color(0xFF0F2027),
    Color(0xFF203A43),
    Color(0xFF2C5364),
  ];

  // Feature Theme (Used for Dashboards, Attendance, Fees)
  static const Color teal = Color(0xFF0C855E);
  static const Color tealDark = Color(0xFF085C41);
  static const Color background = Color(0xFFF7F1E9); // Light cream background from DashboardPage
  static const Color gold = Color(0xFFFFD700);
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
}
